node_modules/

# Freebuff desktop runtime (session DBs and preview logs).
.freebuff/

# Build artifacts — untracked; files stay on disk (Glitch serves frontend/dist from the working copy).
dist/
frontend/dist/
backend/api/

.env
backend/.env
frontend/.env
*.env.recovered
*.recovered
.postgres.local
backend/.postgres.local

*.log
logs/
backend/logs/
.DS_Store
Thumbs.db

uploads/*
!uploads/.gitkeep

# Runtime backups generated by the app.
backups/
backend/backups/
full-backups/

# Local/generated certificates.
backend/certs/*.pem
frontend/certs/*.pem

# Vite/build temporary artifacts.
tmp_served_*.js
*.tmp.js
.tmp-*.vue
vite.config.js.timestamp-*.mjs
*.tsbuildinfo
frontend/ast.txt
eslint_output.txt

# Test/coverage/temp output.
tmp/
scratch/
coverage/
.nyc_output/
test-results/
frontend/test-results/
playwright-report/

# Capacitor/Android generated artifacts (نُسجِّل المشروع نفسه، والمخرجات تُولَّد بـ mobile:sync)
android/app/build/
android/build/
android/.gradle/
android/local.properties
android/app/src/main/assets/public/
android/capacitor-cordova-android-plugins/
*.keystore
!android/app/bn-al-ajouz-keystore.keystore

# ملف كلمات مرور التوقيع — سري ولا يُرفع أبدًا (انظر keystore.properties.example)
android/keystore.properties
